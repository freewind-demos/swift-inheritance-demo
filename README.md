# Swift 继承 Demo

## 简介

展示 Swift 类的继承、多层继承、向下转型。

## 启动和使用

```bash
cd swift-inheritance-demo
swift run
```

## 教程

### 继承语法

```swift
class 子类: 父类 {
    // 子类实现
}
```

### 重写方法

使用 `override` 关键字

### 类型检查

- `is`: 检查类型
- `as`: 向下转型
- `as?`: 可选转型
- `as!`: 强制转型
